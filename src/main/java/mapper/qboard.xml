<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.multi.quizwiki.qboard">
  <!--게시물 등록  -->
  	<insert id="insert" parameterType="qboard">
  		insert into qboard VALUES (#{memberId}, #{title}, #{content},now(), null, #{showCount}, #{likeCount} )
  	</insert>
  
<!--   <select id="selectBoardDetail" parameterType="int" resultType="">
  	select * from qboard where qboard_id =#{qboard_id}
  	
  </select>
  
  <select id="selectall" resultType="QboardDTO">
  	
  
  </select> -->
  
  
  </mapper>